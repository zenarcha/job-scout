/**
 * D-110: a Next.js app on Vercel that reads Supabase directly from the browser via the
 * anon key. There is no server layer and no API route, so there is nothing to configure
 * here beyond the defaults — deliberately minimal rather than pre-emptively featureful.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {};

export default nextConfig;
